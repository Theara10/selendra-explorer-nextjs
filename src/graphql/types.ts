export type Block = {
    eventsCount: number;
    callsCount: number;
    id: string;
    specVersion: number;
    timestamp: string;
    extrinsicsCount: number;
    height: number;
    validator: string;
    parentHash: string;
};